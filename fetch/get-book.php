<?php 
// fetch/get-book.php - This file is commented out and not currently used
// It appears to be a leftover from an older version of the code

/*
  // Load database connection
  include('../config/database.php');

  // SQL query to get all books from the database
  $sql = "SELECT * FROM books";

  // Execute the query
  $result = $conn->query($sql);

  // Create an empty array to store books
  $books = [];

  // If query returned any rows
  if($result->num_rows > 0) {
    // Loop through each book in the result
    while($row = $result->fetch_assoc()){
      // Add the book data to the array
      $books = $row;
    }
  } 
  */
?>  